package utool.plugin;
import utool.plugin.PluginData;

//http://developer.android.com/guide/components/aidl.html

interface IUTooLCore
{
	int startServer();
	void setInitialConnectMessage(String message);
	int getConnectionCount();
	void connectToServer(in byte[] serverAddress, in int port);
	boolean isClient();
	void save(in PluginData data);
	PluginData restore();
	void send(String message);
	String receive();
	void close();
}